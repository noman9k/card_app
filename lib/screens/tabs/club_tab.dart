import 'package:flutter/material.dart';

class ClubTab extends StatelessWidget {
  const ClubTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
